#version 330

uniform mat4 matVP;
uniform mat4 matGeo;
uniform mat4 projMat;

layout (location = 0) in vec4 aParticlePos;
layout (location = 1) in vec4 aParticleColor;

//attribute vec4 aParticlePos;

out vec4 color;

void main() {
   
   //gl_Position = matVP * matGeo * vec4(pos, 1);
   //gl_Position = vec4(pos, 1);
   vec4 realPos = vec4(aParticlePos.xyz, 1.0);
   gl_Position = matVP * matGeo * realPos;
   //gl_Position = pos;
   //gl_Position = vec4(0.0, 0.0, -5.0, 1.0);
   color = aParticleColor;
}
